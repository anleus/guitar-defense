using System;
using Core.Enemy;
using UnityEngine;

namespace Core.Limits
{
    public class BottomLimit : MonoBehaviour
    {
        /*private void OnTriggerEnter2D(Collider2D other)
        {
            if (other.CompareTag("Enemy") && TryGetComponent<EnemyController>(out var enemy))
            {
                Debug.Log("collision");
                enemy.Despawn();
            }
        }*/
    }
}